import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xff7d7a7a);
  static const font = Color(0xff000000);
  static const font2 = Color(0xff000000);
  static const disableFont = Color(0xff000000);
  static const disableButton = Color(0xff000000);
  static const background = Color(0xFFE7CFB6); // Background color, don't change
  static const black = Color(0xff000000);
  static const white = Color(0xffffffff);
}
